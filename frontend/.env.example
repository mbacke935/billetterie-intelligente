# Actuellement, le frontend n'utilise aucune variable d'environnement
# (les URLs des services sont codées en dur dans src/services/*.js
# et proxifiées via vite.config.js en développement).
# Ce fichier est conservé pour une future configuration par environnement.
